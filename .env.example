# ── Supabase (required for production; without these, app uses mock data) ──
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# VITE_USE_SUPABASE_BACKEND=false   # force mock even with creds set

# ── App shell (Seattle MVP launch defaults) ──
VITE_APP_MODE=venue
VITE_ALLOW_VENUE_SHELL=true
VITE_LAUNCHED_CITIES=Seattle,WA
# VITE_APP_MODE=signal               # Pulse Signal research shell only

# ── Feature flags (see docs/feature-flags.md) ──
# Keep unfinished surfaces off for public Seattle launch.
VITE_FF_ENABLE_INTEGRATIONS=true
VITE_FF_ENABLE_SOCIAL_DASHBOARD=false
VITE_FF_ENABLE_SMART_MAP=true
VITE_TICKETING_ENABLED=0
VITE_AI_CONCIERGE_ENABLED=0
# Photo → energy vibe assessment (needs ANTHROPIC_API_KEY on the server)
VITE_VIBE_VISION_ENABLED=0
# VIBE_VISION_MODEL=claude-sonnet-4-6
# VIBE_VISION_DAILY_CENTS_CAP=50
VITE_CREATOR_ECONOMY_ENABLED=0
# VITE_VIDEO_FEED_ENABLED=0
VITE_SAFETY_KIT_ENABLED=0

# ── Maps & observability (optional) ──
# VITE_MAPBOX_TOKEN=pk.eyJ...
# VITE_SENTRY_DSN=https://...
# VITE_ANALYTICS_BACKEND=console

# ── Misc ──
# VITE_RESEARCH_FEEDBACK_URL=https://   # survey / Calendly in Settings

# ── Testing (CI / Playwright) ──
# PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173
# VITE_E2E_AUTH_BYPASS=true

# Production matrix: docs/prod-vercel-env.md
# Full env reference: docs/getting-started.md, docs/secrets-and-integrations.md
