name: Lighthouse CI

on:
  pull_request:
    branches: [main, master]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build production bundle
        run: npm run build

      - name: Install serve
        run: npm install -g serve

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          configPath: ./lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: Check bundle size budget
        # Canonical per-chunk + total gzip budgets (docs/bundle-budget.md,
        # scripts/check-bundle-size.mjs). Replaces the old raw-bytes total
        # check, which double-counted lazy chunks that never affect first paint.
        run: npm run bundle-size

      - name: Report bundle breakdown
        if: always()
        run: |
          echo "## 📦 Bundle Analysis" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Chunk | Size (KB) |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-----------|" >> $GITHUB_STEP_SUMMARY
          for file in dist/assets/*.js; do
            size=$(du -k "$file" | cut -f1)
            name=$(basename "$file")
            echo "| \`$name\` | $size |" >> $GITHUB_STEP_SUMMARY
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          TOTAL=$(find dist/assets -name '*.js' -exec du -cb {} + | tail -1 | cut -f1)
          echo "**Total JS:** $((TOTAL / 1024)) KB" >> $GITHUB_STEP_SUMMARY
